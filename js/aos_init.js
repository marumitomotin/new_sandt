  jQuery(window).on('load', function() {
    AOS.init({
      offset: 200,
      duration: 600,
      easing: 'ease-out-sine',
      delay: 200,
      once: true
      // offset: 200,
      // duration: 800,
      // easing: 'ease-in-sine',
      // delay: 100,
      // disable: 'mobile'
    });
  });
